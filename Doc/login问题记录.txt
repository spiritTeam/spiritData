已完成：++，未完成：--，有疑义+-
++1、激活页面的图标经过验证后位置跑偏了。
++2、所以页面的提交功能在提交之前在验证一下（其实不用验证，只需要在有初始化值的时候验证一下就好）。
--3、经提交后验证码刷新和清除(直接关闭和直接跳转的不用写)。
+-4、忘记密码中的提示要改到按钮上方
	（只把提示移到了提交按钮的上方，但是感觉缩短了高度也不好看了，
		无法通过win.modify方法修改窗口高度，我的新打开了一个窗口，但是新窗口关闭以后，
		原窗口中的功能出现问题。这问题我找了原因，是因为多了一个"#"所以winId跟数组中的id对不上，导致无法操作）。
++4、忘记密码中的提示步骤，客户只能看到一步，其他的看不到，要修改。
+-5、清除所有页面中的不需要的验证图标(清除了部分)。
--6、各个页面的尺寸，有的过大，根据实际大小修改。
--7、有的disable的输入框，样式不一样。
--8、在未激活时，点修改密码后，可以登录。
--9、邮件发送失败(如果捕获到了失败，)这种情况未处理，是算注册成功了？还是为成功？
--10、超链接中的“|”要提出来。
--11、顶端的按钮样式太局促，左边留点空儿。
--12、可以先返回注册信息，然后再发邮件。
--13、修改主界面样式。